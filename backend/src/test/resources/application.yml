spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:training;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: ""

app:
  database:
    type: mysql
  daily-problem:
    initial-sync-enabled: false

